# python-api-scraper
Simple python script which makes a request to DummyJSON and retrieves the product name, price, rating, and availability
To run this script, you need to install the dependencies. Download requests (pip install requests). Then open the script.py file in any text editor (python Script.py)
